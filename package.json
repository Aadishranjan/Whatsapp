{
  "name": "project",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd chatapp && npm run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}